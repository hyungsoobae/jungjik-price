spring:
  application:
    name: jungjik

  datasource:
    url:
    username:
    password:

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true

slack:
  webhook:
    url:

admin:
  api:
    token:
